# File Upload with Multer

## Features

- Upload image files using Multer middleware
- Restrict uploads to images only (jpeg, jpg, png, webp)
- Save files in `/uploads` folder
- Serve uploaded files as static assets
- Return uploaded file path in API response

## Setup & Run

1. Install dependencies:

```bash
npm install
```
